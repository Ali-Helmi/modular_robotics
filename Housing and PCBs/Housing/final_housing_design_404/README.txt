module directories have the SOLIDWORKS files specific to the housing of each module

stl_files directory has the stl files for all FDM optimized components as well as a file, "parts for each module.txt"
that contains info on the parts needed for each module used in the final robot configuration